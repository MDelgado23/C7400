/**
 * Global test setup. @testing-library/react-native v14 ships its matchers
 * built-in, so this file is the place for future global mocks
 * (e.g. expo-audio, network) as the suite grows.
 */
export {};
